interface:
  display_name: "Omni SuperDev"
  short_description: "Single end-to-end engineering mode"
  default_prompt: "Use $omni-superdev as the single end-to-end engineering mode across discovery, planning, implementation, debugging, testing, UX, security, review, and delivery."

policy:
  allow_implicit_invocation: true
