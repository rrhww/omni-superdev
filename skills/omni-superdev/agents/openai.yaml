interface:
  display_name: "Omni SuperDev"
  short_description: "Independent full-cycle R&D workflow"
  default_prompt: "Use $omni-superdev to take this from product intent through technical design, implementation, testing, review, and verification."

policy:
  allow_implicit_invocation: true
